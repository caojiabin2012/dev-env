# v0.0.3

- 修复 `pnpm tauri build` 构建失败问题，清理前端 TypeScript 编译错误
- 打包配置对齐同级 `dev-tools`，Tauri 改为使用 `pnpm dev` / `pnpm build`
- 修复 GitHub Actions 中 artifact action 的 Node 24 兼容问题
- 修复 macOS/Linux 打包失败：将 Windows 专用依赖 `winreg` 限定到 Windows 目标
- 更新器发布地址切换为当前 `dev-env` 仓库的 `latest.json`
- 侧栏颜色与结构继续对齐 `dev-tools`
